'use strict';

describe('myApp.gameboard module', function() {

  beforeEach(module('myApp.gameboard'));

  describe('gameboard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('MancalaBoardCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});